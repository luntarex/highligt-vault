services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vibe-vault-backend
    env_file:
      - .env
    ports:
      - "8080:8080"
    restart: unless-stopped
